<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon" />
  </head>
  <body>
    <div class="container">
      <nav>
        <h1>Mini Message Board</h1>
      </nav>
      <main>
        <h1><%= title %></h1>
        <form action="" method="post" class="message-form">
            <input type="text" name="user" value="<%=typeof message === 'undefined' ? '' : message.user%>" placeholder="Name"/>
            <textarea name="text" placeholder="Type your message here" rows="10" cols="33"><%- typeof message === 'undefined' ? '' : message.text%></textarea>
            <button type="submit" class="submit-form">Submit</button>
        </form>
        <% if (typeof errors !== 'undefined') {errors.forEach(error => {%>
        <li><%=error.msg%></li>
        <%})}%>
      </main>
    </div>
  </body>
</html>
